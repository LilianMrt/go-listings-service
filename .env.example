# Copy to .env for local development.
HTTP_ADDR=:8080
SHUTDOWN_TIMEOUT=10s

# PostgreSQL
DATABASE_URL=postgres://listings:listings@localhost:5432/listings?sslmode=disable

# Kafka
KAFKA_ENABLED=true
KAFKA_BROKERS=localhost:9092
KAFKA_TOPIC=listings.events
